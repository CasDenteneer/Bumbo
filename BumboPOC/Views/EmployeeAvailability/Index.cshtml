@model IEnumerable<BumboPOC.Models.DomainModels.UnavailableMoment>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Employee)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StartTime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EndTime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsAccountedForInWorkLoad)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Employee.FullName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StartTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EndTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsAccountedForInWorkLoad)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.UnavailableMomentId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.UnavailableMomentId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.UnavailableMomentId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
